if SHIELD_SW_SWEEP_LEFT

config ZMK_KEYBOARD_NAME
    default "SW_sweep"

config ZMK_SPLIT_ROLE_CENTRAL
    default y

endif

if SHIELD_SW_SWEEP_LEFT || SHIELD_SW_SWEEP_RIGHT

config ZMK_SPLIT
    default y

endif
